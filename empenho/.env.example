# Segredos do Empenho · Radar de Licitações
# Copie para .env e preencha. NUNCA comite o .env (já está no .gitignore).

# --- Notificação (Fase 2, opcional) ---
# Telegram: crie um bot no @BotFather e pegue o token; chat_id via @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# E-mail (SMTP) — alternativa ao Telegram
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TO=
